steps:

  # Build
  - name: maven:3-jdk-8
    entrypoint: mvn
    args: ['clean', 'package']
    dir: 'backend'
    id: build
    waitFor: [ '-' ]

  # Test
  - name: maven:3-jdk-8
    entrypoint: mvn
    args: ['test']
    dir: 'backend'
    id: test
    waitFor:
      - build
